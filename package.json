{
  "name": "browserify-demo",
  "version": "1.0.1",
  "description": "A working demo of CSS Modules using browserify",
  "main": "index.js",
  "scripts": {
    "test": "npm run build",
    "clean": "rm -rf dist && mkdir dist",
    "browserify": "browserify -t babelify -t brfs -p [css-modulesify -u postcss-modules-local-by-default -u postcss-modules-extract-imports -u postcss-modules-scope -u postcss-color-rebeccapurple -u autoprefixer --autoprefixer.browsers '> 5%' -o dist/main.css] -o dist/index.js src/index.js",
    "watchify": "watchify -v -t babelify -t brfs -p [css-modulesify -u postcss-modules-local-by-default -u postcss-modules-extract-imports -u postcss-modules-scope -u postcss-color-rebeccapurple -u autoprefixer --autoprefixer.browsers '> 5%' -o dist/main.css] -o dist/index.js src/index.js",
    "copy": "cp src/index.html dist/index.html && cp src/components/0-Logo/logo.svg dist/logo.svg",
    "build": "npm run clean && npm run copy && npm run browserify",
    "budo": "budo src/index.js:index.js --dir=dist -- -t babelify -t brfs -p [css-modulesify -u postcss-modules-local-by-default -u postcss-modules-extract-imports -u postcss-modules-scope -u postcss-color-rebeccapurple -u autoprefixer --autoprefixer.browsers '> 5%' -o dist/main.css] | opnr | garnish",
    "start": "npm run clean && npm run copy && npm run budo",
    "deploy": "npm run build && gh-pages -d dist -m \"Updates --skip-ci\""
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/css-modules/browserify-demo.git"
  },
  "author": "Josh Johnston",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/css-modules/browserify-demo/issues"
  },
  "homepage": "https://github.com/css-modules/browserify-demo",
  "devDependencies": {
    "autoprefixer": "^5.2.0",
    "babelify": "^6.1.2",
    "brfs": "^1.4.0",
    "browserify": "^10.2.3",
    "budo": "^4.0.0",
    "css-modulesify": "^0.3.2",
    "garnish": "^2.1.3",
    "gh-pages": "git://github.com/markdalgleish/gh-pages#cli-message",
    "hyperscript": "^1.4.6",
    "opnr": "^1.0.4",
    "postcss-color-rebeccapurple": "^1.1.0",
    "postcss-modules-extract-imports": "0.0.5",
    "postcss-modules-local-by-default": "0.0.10",
    "postcss-modules-scope": "0.0.8",
    "react": "^0.13.3",
    "watchify": "^3.2.2"
  }
}
